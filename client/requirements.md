## Packages
framer-motion | Page transitions and complex micro-interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
- Images use Unsplash URLs for MVP; production would need real file upload
- Price is stored in cents (integer) but displayed in dollars
- Auth uses Replit Auth (via /api/login endpoints)
- "Sell" and "Buy" actions are protected routes
